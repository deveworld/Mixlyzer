# Python artifacts
__pycache__/
*.py[cod]
*.pyd
*.so
*.dylib
*.egg-info/
*.egg
*.log
*.npz
!assets/weights/phrase_analyzer.npz
*.zip

# Virtual envs
.venv/
.env/
env/
venv/
.uv/

# IDE/editor
.vscode/
.idea/

# OS cruft
.DS_Store
Thumbs.db

# Build / packaging outputs
dist/
build/*
!build/*.spec

# User data / caches
library/
debug/
ffmpeg.exe
config.json
*.xml

# dataset file (will be splitted into separate repo.)
annotator/
benchmark/

# Rust build artifacts. Unanchored, so a target/ that cargo creates inside a
# single crate (running it from that directory rather than the workspace root)
# is ignored too, not just the workspace one.
target/
