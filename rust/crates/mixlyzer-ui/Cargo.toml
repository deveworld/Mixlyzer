[package]
name = "mixlyzer-ui"
description = "Mixlyzer's track views drawn with egui: waveform, beatgrid, key strip, phrases, cues"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
mixlyzer-core = { workspace = true }
egui = "0.35"

[dev-dependencies]
image = "0.25.6"
